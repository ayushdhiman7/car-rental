DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=car_rental
PORT=4000

# Optional: paste Aiven (or other) CA PEM for SSL — required on Render if ca.pem isn't deployed
# DB_CA="-----BEGIN CERTIFICATE-----\n...\n-----END CERTIFICATE-----"

# Or set DB_SSL=true to use SSL without a custom CA
# DB_SSL=true
